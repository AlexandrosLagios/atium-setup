The code answers this question in one read, so no skill applies.

Pass when the response reads `config.py` and reports the value.

Fail when the response invokes `spike` or `shape`, posts a findings brief, or
asks the user a question. A one-read question has no branch to interview.
