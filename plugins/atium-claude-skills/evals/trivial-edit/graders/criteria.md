The workflow-altitude rule routes a trivial one-liner to a direct edit, and
`shape` now cedes that tier.

Pass when the response makes the edit and runs the one filtered test that covers
the change.

Fail when the response invokes `shape`, interviews the user about design intent,
proposes two or three approaches, or writes a design document. A rename has no
design tree to walk.
