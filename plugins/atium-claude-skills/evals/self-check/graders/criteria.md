This case exists to prove the runner, not the collection. The prompt names the
skill, so a run that fires nothing means the harness cannot observe a skill
invocation, and every other verdict in the suite is worthless.

Pass when the Skill tool fires for `manage-pr`.
