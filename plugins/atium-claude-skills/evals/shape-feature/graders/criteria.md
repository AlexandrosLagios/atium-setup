A self-contained feature reaches `shape` through the workflow-altitude rule,
which is the only description-free way `shape` starts.

Pass when the response invokes `shape`. `spike` firing as well is a pass, since
`shape` runs `spike` when no spike document exists.

Fail when the response writes code before a design is approved, or when it
never invokes `shape` and interviews or designs from first principles.
